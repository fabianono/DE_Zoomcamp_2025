version: 2

sources:
  - name: staging
    database: "{{ env_var('DBT_DATABASE', 'my-project-57990-1714709310544') }}"
    schema: "{{ env_var('DBT_SCHEMA', 'tripdata_all') }}"
      # loaded_at_field: record_loaded_at
    tables:
      - name: green_tripdata
      - name: yellow_tripdata
         # freshness:
           # error_after: {count: 6, period: hour}

